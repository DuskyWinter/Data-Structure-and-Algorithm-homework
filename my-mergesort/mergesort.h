#ifndef __mycode__
#define __nycode__
#include <vector>
#include <limits>

int Merge(std::vector<int> &_A, int _p, int q, int _r);

int _MergeSort(std::vector<int> &_A, int _p,int _r);

int MergeSort(std::vector<int> &_A);

#else
//Do Nothing
#endif

